function lf --wraps=yazi --description "Open Yazi"
    # Maintainer Preference: use the short `lf` name for Yazi. Do not adopt this
    # when the user has the lf file manager or another command with that name.
    yazi $argv
end
